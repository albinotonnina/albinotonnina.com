(() => {
  module.exports = (scrollyTelly) => {
    const init = (scrollyTelly) => {
      scrollyTelly.menu = {};
      scrollyTelly.menu.init = () => {};
    };

    init(scrollyTelly);
  };
})(document, window);
